# puka — top-level test entry (referenced by cyrius.cyml [build].test).
# For unit tests, prefer adding cases to tests/puka.tcyr.

include "lib/syscalls.cyr"

fn main() {
    return 0;
}

fn _entry(): i64 { var rc = main(); syscall(SYS_EXIT, rc); return 0; }
_entry();
